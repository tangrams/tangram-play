machine:
  node:
    version: 4.3.1

dependencies:
  post:
    - npm install -g gulp-cli && gulp build

test:
  override:
    - npm run lint

general:
  artifacts:
    - "build"
    - "data"
    - "index.html"
