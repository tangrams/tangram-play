'use strict';

export default class Widget {
    constructor (cm, proto, content, nline) {
        this.type = what.type;
    }
}

// TODO: Build a generic class widget
// All other widget types extend it
